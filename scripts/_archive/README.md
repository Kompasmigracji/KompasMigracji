# Archived one-off scripts

Ad hoc scripts run manually against a live DB/deploy during earlier development
(schema checks, seed data, translation fixes, webhook tests). Not part of the
build, `package.json` scripts, or test suite — see the "Repo hygiene notes"
section of `CLAUDE.md`. Kept for reference, moved out of the repo root.
