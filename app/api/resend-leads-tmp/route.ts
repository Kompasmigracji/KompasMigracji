export const dynamic = 'force-dynamic';

import { NextResponse } from 'next/server';
import { notifyAdmin } from '@/lib/telegram';

export async function GET() {
  try {
    const projectInfo = `🏢 <b>ПРОЄКТ «KOMPAS MIGRACJI»: ЯК ЦЕ ПРАЦЮЄ В ЦІЛОМУ</b>

Наша система — це не просто сайт, це повноцінний «віртуальний офіс», який працює 24/7. Він складається з 4 головних частин:

<b>1. Збір клієнтів (Штучний Інтелект)</b>
У нас працюють AI-боти (Оракул та Мілена) у Telegram, Viber, Facebook, Instagram та на сайті. Вони самостійно консультують людей, відповідають на базові питання, з'ясовують проблему і беруть номер телефону.

<b>2. CRM-система (Робоче місце команди)</b>
Щойно бот бере номер телефону, він миттєво створює картку клієнта (лід) у нашій базі. В адмін-панелі ви бачите всіх клієнтів, звідки вони прийшли, що їм потрібно, і можете міняти статуси їхніх справ ("В роботі", "Оплачено", "Відмова").

<b>3. Клієнтський портал</b>
Особистий кабінет клієнта. Щоб люди не обривали телефони питанням "А на якому етапі моя справа?", вони заходять на портал за своїм PIN-кодом і бачать статус, який ви встановили в CRM.

<b>4. Фінанси та Автоматизація</b>
Система сама приймає гроші, сама видає чеки, сама надсилає нагадування про оплату або дедлайни по документах. Усі дані збираються для майбутньої масової розсилки (маркетингу) та бухгалтерії.`;

    const paymentInfo = `💳 <b>ЯК ПРАЦЮЄ ПРОЦЕС ОПЛАТИ (КРОК ЗА КРОКОМ)</b>

Процес повністю автоматизований, щоб виключити людський фактор:

<b>Крок 1: Виставлення рахунку</b>
Клієнт отримує посилання на оплату (від бота під час розмови або від менеджера вручну). Сума і послуга вже зафіксовані.

<b>Крок 2: Транзакція</b>
Клієнт переходить за посиланням і платить через Przelewy24 (Blik, картка, банк) або Stripe. 

<b>Крок 3: Магія на фоні (Webhook)</b>
Як тільки банк каже "Гроші є", платіжна система (Przelewy24) відправляє невидимий сигнал на наш сервер. Вам не треба нічого перевіряти вручну.

<b>Крок 4: Автоматичні дії системи</b>
Щойно сервер отримує цей сигнал, він за секунду робить 4 речі:
✅ Змінює статус цього клієнта в CRM на "Оплачено".
✅ Створює для клієнта PIN-код від Порталу.
✅ Відправляє клієнту квитанцію та PIN-код на Email / Messenger.
✅ Відправляє у цю Telegram-групу сповіщення ("💳 Нова оплата!") з усіма деталями клієнта.

<b>Крок 5: Робота</b>
Менеджер просто бачить сповіщення в Telegram, бере клієнта в роботу і змінює статуси на Порталі. Всі гроші автоматично фіксуються в статистиці.`;

    await notifyAdmin(projectInfo);
    await new Promise(resolve => setTimeout(resolve, 1000)); // пауза між повідомленнями
    await notifyAdmin(paymentInfo);
    
    return NextResponse.json({ success: true, message: "Project and Payment info sent to Telegram" });
  } catch (err: any) {
    return NextResponse.json({ success: false, error: err.message });
  }
}
